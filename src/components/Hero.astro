---
interface Props {
  headline: string
  lede: string
}
const { headline, lede } = Astro.props
---
<main class="hero">
  <h1 class="headline">{headline}</h1>
  <hr class="rule" />
  <p class="lede">{lede}</p>
</main>
