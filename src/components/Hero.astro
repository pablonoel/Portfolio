---
interface Props {
  headline: string
  lede: string
}
const { headline, lede } = Astro.props
---
<header class="hero">
  <h1 class="headline">{headline}</h1>
  <hr class="rule" />
  <p class="lede">{lede}</p>
</header>
